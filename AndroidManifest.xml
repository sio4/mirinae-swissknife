<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="org.scinix.android.mirinae.swissknife" android:versionCode="1"
	android:versionName="1.0">
	<application android:icon="@drawable/icon" android:label="@string/app_name">
		<activity android:name=".SwissKnife" android:label="@string/app_name"
			android:theme="@android:style/Theme">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

		<activity android:name=".SwissKnifeApps" android:label="@string/app_name">
			<intent-filter />
		</activity>

		<activity android:name=".SwissKnifeTweak" android:label="@string/app_name">
			<intent-filter />
		</activity>

		<activity android:name=".SwissKnifeRTether" android:label="@string/app_name">
			<intent-filter />
		</activity>

	</application>
</manifest> 